namespace _Soru10;

class Program
{
    static void Main(string[] args)
    {
        // Console.WriteLine("Birinci tarihi giriniz (yyyy,mm,dd)" );
        // DateTime tarih1 = DateTime.Parse((Console.ReadLine()));
        // Console.WriteLine("İkinci tarihi griniziz(YYYY,mm,dd)");
        // DateTime tarih2 = DateTime.Parse(Console.ReadLine());
        // TimeSpan fark = tarih1 - tarih2;
        // int gunFarki = Math.Abs(fark.Days);
        // Console.WriteLine($"İki tarih arasındaki gün farkı: {gunFarki} gün");
    }
}
